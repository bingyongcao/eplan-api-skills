using Eplan.EplApi.ApplicationFramework;

namespace __ROOT_NAMESPACE__;

// Verify the IEplAction contract against the selected EPLAN API version before deployment.
public sealed class __CLASS_NAME__ : IEplAction
{
    public bool Execute(ActionCallingContext callingContext)
    {
        // Add the smallest version-verified operation here.
        return true;
    }

    public void GetActionProperties(ref ActionProperties actionProperties)
    {
    }

    public bool OnRegister(ref string name, ref int ordinal)
    {
        name = "__ACTION_NAME__";
        ordinal = 20;
        return true;
    }
}
